# HW 3 MWAA

teammates:
Nathaniel Stall
Didier Munezero

MWAA environment:
`MyAirflowEnvironment`

bucket:
`stall-munezero-final-project`

We started with environment and bucket names that matched the spec, but we had to create 3 different environments to troubleshoot,
and I guess the one that worked accidentally didn't get a correct name written to it. Even after trying to have dinglin 
modify our `stall-munezero-mwaa` bucket permissions, we couldn't access the bucket, so we just used our final project bucket.

the dag we are using on our airflow environment is:
`sm_hw3_dag.py`
